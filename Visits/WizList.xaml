<Window x:Class="Visits.WizList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Visits"
         xmlns:conv="clr-namespace:Visits.Converters"
        mc:Ignorable="d"
        Title="Twoje wizyty" Height="519.626" Width="608.411" Loaded="Window_Loaded">
    <Window.Resources>
        <conv:BoolVisibilityConverter x:Key="visibilityBool"/>
        <conv:ReversedBoolVisibilityConverter x:Key="rvisibilityBool"/>
    </Window.Resources>
    <Grid>
        <Button x:Name="button" Content="Planowane" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="115" Height="38" Command="{Binding Change1}"/>
        <Button x:Name="button1" Content="Archiwalne" HorizontalAlignment="Left" Margin="462,10,0,0" VerticalAlignment="Top" Width="115" Height="38" Command="{Binding Change2}"/>
        <GroupBox x:Name="groupBox" Header="Planowane" HorizontalAlignment="Left" Margin="10,53,0,10.4" Width="567">
            <StackPanel>
                <ListView x:Name="Listawizyt" Visibility="{Binding Who, Converter={StaticResource rvisibilityBool}, ConverterParameter=Inverted, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="178" Margin="10,10,-3.4,-3.2" VerticalAlignment="Top" Width="547" ItemsSource="{Binding Wat}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Doctor.User.Name }" Header="Nazwisko lekarza"/>
                            <GridViewColumn DisplayMemberBinding="{Binding Doctor.Specialization}" Header="Specjalizacja"/>
                            <GridViewColumn DisplayMemberBinding="{Binding Date, StringFormat=\{0:dd.MM.yyyy hh:mm\}}" Header="Data Wizyty"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <ListView x:Name="ListaWizyt1" Visibility="{Binding Who, Converter={StaticResource visibilityBool}, ConverterParameter=True, UpdateSourceTrigger=PropertyChanged}"  Height="219" Margin="0,0,-3.4,0" HorizontalAlignment="Right" Width="547" ItemsSource="{Binding Wat}" >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Patient.User.Name }" Header="Nazwisko Pacjenta"/>
                            <GridViewColumn DisplayMemberBinding="{Binding Date, StringFormat=\{0:dd.MM.yyyy hh:mm\}}" Header="Data Wizyty"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </GroupBox>

    </Grid>
</Window>
